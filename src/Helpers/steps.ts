export const isFirstStep = (currentStep: number): boolean => currentStep === 0;
export const isSecondStep = (currentStep: number): boolean => currentStep === 1;
export const isThirdStep = (currentStep: number): boolean => currentStep === 2;
